namespace BFC.Core.Formations
{
    /// <summary>
    /// Logical role used by formation and rules code. Presentation must not infer role from visuals.
    /// </summary>
    public enum PieceRole
    {
        Outfield = 0,
        Goalkeeper = 1
    }
}
